 * bodies feel like they fade too much when zooming out
 * still lot's of textual explanations about design decisions
 * implement something like URLs, slugs or similar that allows for
   easy definition and sharing of view points, for example like
   "looking at the earth staying on the ecliptic, keeping the direction
   and distance", then the view would change accordingly and once animation
   starts, the view would follow the earth